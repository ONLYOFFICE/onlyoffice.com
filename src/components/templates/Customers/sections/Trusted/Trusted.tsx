import { Section } from "@src/components/ui/Section";
import { Container } from "@src/components/ui/Container";
import { Heading } from "@src/components/ui/Heading";

const Trusted = () => {
  return (
    <Section>
      <Container>
        <Heading label="Trusted" size={2} level={2} />
      </Container>
    </Section>
  );
};

export { Trusted };