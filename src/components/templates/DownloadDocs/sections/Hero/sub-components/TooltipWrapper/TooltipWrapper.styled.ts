import styled from "styled-components";

const StyledTooltipWrapperText = styled.span`
  border-bottom: 1px dashed #ff6f3d;
  color: #ff6f3d;
  cursor: pointer;
`;

export { StyledTooltipWrapperText };
